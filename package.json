{
  "name": "bank-dxp",
  "version": "0.1.0",
  "private": true,
  "description": "Bank Digital Experience Platform MVP monorepo",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run dev -ws --if-present",
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "test": "npm run test -ws --if-present",
    "clean": "rimraf dist services/*/dist",
    "demo": "tsx scripts/demo-flow.ts"
  },
  "workspaces": [
    "services/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/node": "^20.11.19",
    "@typescript-eslint/eslint-plugin": "^7.0.2",
    "@typescript-eslint/parser": "^7.0.2",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "prettier": "^3.2.5",
    "prisma": "^6.19.0",
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "undici": "^6.19.6"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0"
  }
}
